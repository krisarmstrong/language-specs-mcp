package goroot // import "internal/goroot"


FUNCTIONS

func IsStandardPackage(goroot, compiler, path string) bool
    IsStandardPackage reports whether path is a standard package, given goroot
    and compiler.

